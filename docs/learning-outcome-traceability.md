# Learning-outcome traceability (D1–D7)

Maps concrete evidence (files, experiments, commits) to the assessed learning outcomes. Updated as evidence is produced, never retroactively fabricated. **Phase 0 baseline below; every milestone appends.** Last appended: **M9 / research report — chapters authored, M9.10 gate corrections applied 2026-08-11**. (The M8 evidence was appended on 2026-08-09; the header had said M7 until this correction.)

| Outcome | Definition | Evidence so far (Phase 0 → M3) | Planned evidence |
|---|---|---|---|
| D1 — Independent applied research | Formulating and executing own research | `docs/01-research-plan.md` (primary RQ + RQ1–RQ12 with hypotheses/methods); **M2: custom 148-item dataset built and documented (RQ3), pipeline answers size/quality prep (RQ4)**; **M3: first executed experiments — EXP-001…EXP-005 in `experiments/registry.csv`, answering RQ2 and RQ8 with measured VRAM/latency data, including two hypotheses the student's own evidence REFUTED (direct tall generation; thermal throttling as the cause of a timing anomaly)** ; **M4: EXP-007…EXP-014 answering RQ6 and part of RQ7 across 299 measured generations with zero failures, including a factual error in the approved plan caught by opening the actual reference image and corrected before anything was built on it**; **M5: EXP-016…EXP-019 answering RQ1 with measured training data — 13 runs across 8 experiments, including the mandatory R12 combined-stack acceptance item, and a hypothesis the student's own reasoning corrected before execution (gradient accumulation is not a memory-saving tier at micro-batch 1), later confirmed by the measured phase-separated peaks** ; **M6: EXP-020…EXP-033 answering RQ4 and RQ5 across 10 training runs and 360 measured generations, including a hypothesis that had to be REWRITTEN before it could be tested ("at least as strong" cannot be refuted by equality, so it was replaced by four explicit verdict rules) and a size result the student's own evidence left INCONCLUSIVE (O5) with a non-monotonic ordering, rather than forced into a tidier story** ; **M7: EXP-034 and EXP-035 answering two questions nobody had asked yet — whether the stack survives as a long-lived service, and whether the placeholder the prompt-only path depends on can influence a result. Both had pass criteria declared in code before execution, and EXP-034 deliberately breaks the project's own one-config-per-process measurement rule, stating in the record that its figures are therefore NOT comparable with EXP-016…EXP-032** | Executed experiments (EXP-###), prototype conclusions |
| D2 — Independent professional functioning | Autonomous, structured, responsible work | Environment audit before decisions; risk register R1–R11; security foundation; `docs/ai-usage.md` boundary log; **M2: human approval gate before any download, two blocked sources handled within the approved registry (conditions A/B), honest curation log**; **M3: gated base model (HTTP 401) escalated for a decision rather than authenticated or silently substituted; mandatory human-review gate held before any conclusion was written; pip-22.3 install blocker diagnosed and documented rather than worked around**; **M5: a new dependency installed only after its resolver report was parsed and shown to move none of the five pinned packages, with a pre-declared stop-and-ask condition; `bitsandbytes` left uninstalled rather than added silently; hard wall-clock limits and a projection gate enforced before any longer run started** ; **M6: the milestone stopped dead at a human gate with the pilots finished and nothing concluded, and stopped again at gate 2 with a draft decision record and no conclusion; the trigger tokens in the student-approved plan were rejected on measured tokenizer evidence BEFORE any GPU time was spent on them; three defects in the assistant's own work were found, recorded and fixed rather than quietly patched** ; **M7: a declared cap of 25 GPU generations was reached exactly and then respected — when the review gate needed fit-mode screenshots after the cap, an existing decal was loaded from disk rather than a 26th generated, and the reason is recorded in the evidence; four new dependencies were installed only after a parsed resolver report proved none of the ten protected ML pins would move, and a starlette deprecation warning was left unfixed rather than risk one** ; **M8: a clean-clone test was run as a real test rather than a formality, and its first failure was taken to a human gate instead of being fixed unilaterally - the fix touched a value documented as frozen across M6 evidence, so it was Kylian's to authorise. Two further findings (unpinned transitive dependencies, three pre-existing npm advisories) were recorded and deliberately NOT fixed under the feature freeze, with the reason stated. A CI workflow was committed but never pushed, and its evidence record opens by saying it has never run** | Milestone delivery against plan; blocker handling in process log |
| D3 — Iterative planning and professional methodology | Planning, adjusting, and documenting method | Planning v1 (M0–M11) with preserved-original rule; process log; git workflow; atomic commit protocol ; **M6: the milestone ran as a two-gate loop rather than one - plan, pilots, STOP, human decisions, approved execution, STOP, human decisions, conclusion - and both gates are documented with what was deliberately NOT done at each** ; **M7: the plan itself went through two rounds of student corrections before any code was written — twelve mandatory corrections after the first draft and two after the second — which is what added the frozen 12-request residency matrix, the single-process invariant, and the rule that a 504 may only be returned once GPU work has actually stopped** ; **M8: the approved plan was corrected by its own execution rather than followed to the letter - a planned CI skip-marker for checkpoint tests was dropped after checking showed the suite already passes with the weights removed, and two planned duplicate security tests were dropped because M7 already covered them. Adding either would have inflated the test count while implying coverage that did not change. A feature freeze was recorded with its allowed and forbidden changes, and brought forward from the calendar date to the day the work actually finished** | Planning change log entries with reasons; per-milestone process-log entries |
| D4 — Comparison and application of multiple solution methods | Weighing alternatives with criteria | Weighted matrices D-A…D-D + DR-001…004; ML feasibility screening kept as hypothesis; **P0: four viewer approaches compared with weighted criteria → DR-005, validated by working software**; **P1: base models compared on MEASURED data (matrix D-E, the first populated with real hardware figures) → DR-007, with a two-track design (controlled 512 px vs each model's native resolution) that prevented an unfair comparison, plus four deck-format strategies compared → direct 1:3 selected** ; **P2: four conditioning arms compared on measured data (matrix D-F) → DR-008, with a two-phase design that kept the 2.35 GiB metric encoder out of every process whose VRAM it would have inflated, and ControlNet compared on criteria but deliberately not implemented with the screen-out reason recorded**; **P3: the five assignment-mandated fine-tuning methods weighed against five criteria → DR-009, which selects LoRA on MEASURED feasibility and states explicitly that it does NOT claim superiority over DreamBooth, Textual Inversion or full fine-tuning, because those were screened and never run — the same honest limitation DR-007 carries for the gated SD 2.1** ; **P4: caption strategy compared as a blinded A/B changing exactly one variable, dataset size compared across nested 12/24/44 subsets at equal compute with the repetition confound declared IN CODE before any result existed, and per-style versus multi-style compared with per-style exposure matched rather than a shared total divided three ways — the naive "same total steps" comparison would have given each style a third of the exposure and quietly answered a different question** ; **P5: two texture-fit strategies for the 1:3 decal against the 1:3.902 deck were both BUILT rather than one being argued for, each disclosing its cost numerically (1.3008× stretch versus 23.12 % bare deck), with the selection left to the human gate and a test asserting no default is exported — **at the gate on 2026-08-07 Kylian selected `full-surface` and supplied his own written rationale, recorded verbatim in DR-012; the rejected mode was kept selectable rather than deleted, and two further options (regenerating at 512×1998, reshaping the deck to 1:3) are recorded as screened-not-measured with the reason, because each would invalidate evidence an earlier decision rests on**; two ways of serving prompt-only requests were compared against the diffusers source before either was implemented, and the resident-at-scale-0.0 option was chosen over unloading the adapter because the alternative discards a 2.35 GiB encoder on every toggle** | Empirical method comparisons (RQ1, RQ5, RQ6) answered for RQ6 in Prototype 2; RQ1/RQ5 remain for Prototypes 3–4 |
| D5 — Complex problem solving via prototypes and new technologies | Multi-prototype iterative build-up | Prototype ladder defined (`docs/06-prototype-overview.md`); **Prototype 0 completed: RQ9 answered, 13 passing tests, real problems (jsdom/Node, stale canvas captures) diagnosed and solved — `docs/prototypes/prototype-0.md` + `docs/evidence/prototype-0/`**; **Prototype 1 completed: RQ2/RQ8 answered, 66 passing tests, first ML runtime; three hard problems diagnosed — a silent WDDM host-memory spill masking an 8 GB overflow as "success", an instrumentation bug caught by the mandated smoke test, and a caching-allocator measurement contamination whose obvious cause was tested and refuted — `docs/prototypes/prototype-1.md`** ; **Prototype 2 completed: RQ6 answered, 123 passing tests, reference conditioning built on a pinned adapter; three real problems diagnosed — a diffusers pinning gap that would have loaded 2.35 GiB of encoder weights from a moving branch, a runner defect that would have crashed the text-only baseline arm at launch, and a reporting bug in my own analysis that presented missing data as a failed check**; **Prototype 3 completed: RQ1 answered, 210 passing tests, first training runtime — LoRA trains, saves, reloads and measurably changes generation, and the combined SD 1.5 + LoRA + IP-Adapter stack was proven to fit the deck format by 202 MiB; a defect in the milestone's own combined-stack runner was caught, fixed, and preserved as a failed row rather than deleted** ; **Prototype 4 completed to its technical criteria: RQ4/RQ5 evidence gathered, 284 passing tests, 10 of 10 training runs passing every gate; four real problems diagnosed — wrong trigger tokens caught against the live tokenizer, unseeded LoRA initialisation identified from the sqrt(2) shape of the discrepancy rather than guessed at, 24 duplicate generations in the assistant's own matrix that were biasing a diversity indicator toward zero, and a line-ending rule that would have invalidated a hash-locked human-scoring artifact on checkout** ; **Prototype 5 built and validated: the full flow works end to end — prompt and optional reference → generation → 3D deck preview → download — with 371 pytest and 66 vitest tests, validated against a real uvicorn process rather than a test client. Three real problems diagnosed: diffusers raises on a prompt-only request while IP-Adapter is resident (found by reading the library, not by hitting it in production), Prototype 0's 512×2000 test decals had concealed the deck-geometry mismatch for four milestones, and the assistant's own deadline assertion was wrong where the service was right** ; **Prototype 5 COMPLETE 2026-08-07 at a final human visual gate, 12 of 12 manual acceptance items PASS — 406 pytest and 165 vitest tests, three decision records (DR-011/012/013), and a production feature added at the gate (`Upload your own decal`) that is wholly local and proven so from server-side request counts rather than by assertion. Three further defects were diagnosed in the interface pass, none of them visible to any test: a 70 px viewport overflow, a page-wide horizontal scrollbar from a CSS specificity conflict, and a polling loop restarting on every render** ; **M8: the automated browser layer M7 explicitly did not have - 37 Playwright scenarios against the built frontend with the API mocked at the network boundary, plus a pytest that validates those fixtures against the real Pydantic models so the mock cannot rot into theatre. Five real defects were diagnosed, every one invisible on the machine the code was written on: a frozen dataset hash that had only ever passed locally, a settings file documenting five variables nothing reads, a stale audited Node version, a requirements file claiming a pin it did not make, and `uvicorn --workers 1` starting two processes so a naive stop strands the worker on port 8000** | Prototypes 4–5 implemented with evidence |
| D6 — Justified research conclusions | Conclusions traceable to evidence | Experiment methodology + rubric (`docs/05-experiment-methodology.md`); honesty rules; **M2: every dataset item traces to a licence-verified source in the manifest; rejections traced in the curation log**; **M3: DR-007 cites measured figures and the student's own scores, with Track A and Track B reported separately and never averaged; the student's scores are recorded at the granularity actually used (aggregate per model/track), unreviewed cells marked "not individually scored", and `diversity_across_seeds` left unscored with the reason stated rather than filled in; three failures recorded as first-class results (EXP-003 blocked, sampler bug, measurement contamination)** ; **M4: DR-008 reports objective measurements and human scores in separate sections and never blends them; 29 score cells are recorded as NOT SCORED and excluded from every mean rather than zeroed; text-only at 512×1536 is left unscored rather than reusing an M3 value; the selected method's own memory risk and the rejected method's near-copy behaviour are both stated prominently instead of being softened**; **M5: every technical claim is a gate read back from the live object rather than inferred from a call returning without error — trainable parameter counts, frozen base weights, finite non-zero gradients and a float64 parameter hash before and after; noise thresholds were declared before any result was read; the 202 MiB combined-stack margin is reported against the 8187.5 MiB ceiling and explicitly NOT called comfortable headroom** ; **M6: the gate-1 scores were fixed and hashed BEFORE the blinding map was opened, verified twice, and are now guarded by a pytest, so "no score was edited after unblinding" is checkable rather than asserted; DR-010 is left as a DRAFT WITH NO DECISION because every question remaining in it is a visual judgement; the automated indicators are stated to decide nothing and are kept in separate files from human judgement; the 202 MiB margin is again reported against the ceiling and not called comfortable** ; **M6 gate 2: DR-010 was finalised only after Kylian's scores existed, and its conclusion cites them; six limitations are carried into the record rather than softened, including that zero near-copy flags is not proof of no memorisation and that the labelled gate-2 sheets carry an expectation effect the blinded gate-1 sheets did not** ; **M7: the headline memory figure was revised in the tighter direction on its own evidence — EXP-034 measured a worst spare of 200.0 MiB against the 202.0 MiB quoted since M5, and the register, DR-011 and the planning log all now carry 200.0 rather than the friendlier number; byte-identical repeats are reported as strong evidence of no state residue and explicitly not as proof of none; EXP-035 states that it does NOT re-establish text-only equivalence, which still rests on M4's separate result; and DR-011 is accepted with the texture-fit mode deliberately left open** ; **M7 gate: DR-012 quotes Kylian's rationale verbatim rather than paraphrasing it, and states the honest scope of the evidence behind it — one reviewer, one decal, one camera, and a judgement scoped to the three production styles at 512×1536, not a general claim that a 1.3× stretch is imperceptible; the milestone was NOT closed on the strength of the one answer, because the manual acceptance checklist is recorded as unwalked** ; **M7 interface pass: the loading experience refuses to invent a number where none is measurable — only denoising has a real denominator, so model loading, decoding and saving publish a stage name and a null estimate, and a test enumerates every non-denoising stage to assert no percentage is produced for it; 100 % is claimed only once the PNG has decoded in the browser, not when the last diffusion step lands; the nine interface screenshots are labelled as mocked telemetry with an explicit instruction not to cite their numbers; and the walkthrough's prompt-adherence finding is preserved as a limitation rather than answered by silently rewriting the user's prompt** ; **M8: the clean clone reproduced M7's Phase A output byte-for-byte, and the record states precisely what that does and does not mean - inference is deterministic given a fixed adapter, which does NOT contradict R14, because R14 concerns training. The run's 218.0 MiB spare is recorded as the prompt-only figure and explicitly does not supersede the 200.0 MiB reference-conditioned ceiling. The generation total is reported as 27 rather than rounded back to 25, with the extra run classified as deployment validation carrying no EXP-### id. The CI evidence states it has never run; the deployment DR states Docker was screened out and never benchmarked; and the E2E report states that no suite in the project loads the model** | Registry-backed conclusions; failure records; report chapters |
| D7 — Professional multimedia documentation and presentation | Professional English documentation and presentation | Complete Phase 0 documentation set; README; this traceability system ; **M6: two hash-locked human scoring artifacts, a Gate-1 and a Gate-2 approval record with explicit provenance for who decided and what assisted, and a review-only ZIP built with an extension allowlist so weights and full-resolution outputs cannot be distributed by accident** ; **M7: a gate handover written for a reader rather than for the author — start commands, real health output, real test counts, runtime checkpoint hashes, failure-and-recovery evidence, side-by-side fit screenshots and a twelve-item manual acceptance checklist, ending with an explicit list of what was deliberately NOT done; the application itself surfaces the reproducibility metadata and the `retro-poster` limitation to the user rather than confining them to documentation** ; **M7 interface pass: one design-token system replacing scattered values, a single status-message pattern in which meaning never rests on colour alone, review-only evidence tools hidden from the production interface but preserved for traceability, WCAG-AA contrast fixed at its cause rather than per-component, `prefers-reduced-motion` honoured without losing any information, and nine labelled interface screenshots covering idle, cold load, denoising, finalising, success, error and four viewport widths** ; **M8: a deployment runbook written for someone who does not have this machine - prerequisites, ordered install, timings, troubleshooting, and a plain statement that there is no CPU fallback and that three required files cannot be regenerated; a weights manifest guarded by pytest so it cannot drift from the code; a 4-minute timed demo script built around talking over the generation rather than padding it; and a four-rung backup plan in which every asset already exists and the one that does not - a screen recording - is listed as missing rather than assumed** | Research report PDF, presentation PDF, demo, jury preparation |

## M10 — content review (2026-08-14)

| Outcome | evidence |
|---|---|
| **D1 / D6** | The deck claimed **both** human gates were blinded. `GATE-2-approval.md` says the opposite in its own words — Gate 2's sheets were **labelled**, because choosing which checkpoint ships cannot be done blind — and it already recorded the resulting expectation effect. The **evidence was corrected against, not reinterpreted to fit**, and the wording now distinguishes the two gates |
| **D5** | The deck stated the card total, **peak allocated** and **spare** as though the first two produced the third. `8187.5 − 5143.73 = 3043.77`, not 200: allocated counts live tensors, while the margin is measured against **total device occupancy** (7987.5, which subtracts exactly). **Every number was individually true and correctly fact-locked; the relationship implied between them was false** — a class of error a fact-lock system structurally cannot see. `worst_device_used_mib` is now locked so the arithmetic is checkable |
| **D3** | The error was **introduced by the 26→15 merge**; the 26-slide deck had said "blinding at the first gate" and was correct. **Compression is where a qualified claim loses its qualifier**, which is the exact risk `validate_slides.py` was built for — and it still took a human reader, because the concession list checks only the claims it was told to check |
| **D2** | The timing bound was **tightened** (1:04 → 1:49 minimum buffer) and the deck cut to meet it, out of notes rather than slides and at an unchanged 130 wpm. Recorded explicitly as the one case where moving a bound was right, because tightening a check to demand better work and loosening one to admit failing work look similar in a diff |

**M10 is not complete.** Structure approved; **content gate open, rehearsal never run.**

## M10 — the 20-minute restructuring (2026-08-14)

| Outcome | evidence |
|---|---|
| **D2** | The deck compresses a 91-page report into ~15 minutes of speech. `validate_slides.py` is weighted for exactly that risk — a denylist of wordings this project has ruled wrong, and a requirement list of concessions that must survive onto the slide making the claim. **The merged `limits` slide is held to all three bounds its three predecessor slides carried** |
| **D3** | The authoritative slot arrived after the deck was built. The response was to restructure and record why (**DR-017**), not to speak faster: four alternatives were rejected with their arithmetic stated, including the one that would have needed ~220 wpm |
| **D5** | **Both new guards were watched failing before they passed.** The first restructured draft came in at 17.7 min with a negative buffer and the validator refused it. A guard nobody has seen fail is not a guard |
| **D6** | **Every structural check passed on a deck that did not fit.** The checks were sound; the binding constraint was absent from the repository, so nothing could fail on it. Recorded as the fourth instance of one recurring pattern rather than fixed quietly — and it showed that DR-016's tiering was inadequate on its own, since only 4 of 26 slides were cuttable against an ~11-minute overrun |
| **D7** | 15 slides, one central claim each, with the layout budgets **tightened** so that is enforced rather than trusted. Nothing was deleted: what left the visible slides is in the speaker notes and `docs/presentation/jury-questions.md`, structured for 30–60-second answers with evidence paths |

**What M10 still does not evidence.** **No rehearsal has been run**, so 14:56 is an estimate from
note word counts at a chosen 130 wpm and not a measured delivery. The buffer is 1:04 — 5 % of the
slot. **The human timing and visual gate has not been held.**

## M10 — defence presentation, first build (2026-08-11 / 08-14)

| Outcome | M10 evidence |
|---|---|
| **D1** | The deck's closing slide names five conclusions the project **did not** draw — that LoRA beats the alternatives, that SD 1.5 is the best base model, that three styles is enough, that any image count is a threshold, that a green suite means good pictures — and slide 17 concedes there was **no second independent human rater**, so no inter-rater agreement can be reported |
| **D2** | The deck compresses a 91-page report onto 26 slides, and `scripts/validate_slides.py` is weighted for exactly that risk: a **denylist** of wordings this project has ruled wrong and a **requirement list** of concessions that must survive onto the slide making the claim. A slide fails for saying too little as readily as for saying too much |
| **D3** | **DR-016** was written *after* the pipeline it governs, and the record says so in its opening section rather than being back-dated — four files had cited a decision record that did not exist for three days |
| **D4** | DR-016 screens six presentation options, states that **only one was measured**, and records why the chosen one won on a criterion the others could not meet: the deck holds **no facts of its own**, so a number corrected in the report is corrected on the slide by the same edit |
| **D5** | The build refuses to produce a deck it cannot trust: per-layout text budgets are **hard failures**, a slide without a speaker note is a **build failure**, and PDF page count must equal slide count — the direct overflow test. Writing DR-016 **broke the build** by moving a fact lock, which is the mechanism working |
| **D6** | **The committed PDF did not match its sources** — about 20 % of the deliverable was missing and nothing in the repository could report it. Found by verifying rather than trusting, and written up as the third instance of one recurring class: M8's hash that only passed locally, M9's presentation-level bibliography defect, M10's stale artifact |
| **D7** | Two PDFs from one source set — a 16:9 deck and an A4 speaker-note handout — built at **zero new dependencies** under an active feature freeze, with the speaking time reported as an **estimate with its method attached** and never as a rehearsal |

**What M10 does not evidence.** Every check above is structural. **No human visual gate has been
held**, so nothing here is a judgement about legibility, design, or whether the argument survives
compression onto a slide. **No rehearsal has been run**, and **no authoritative presentation duration
is recorded anywhere in this repository**, so the deck's length is unvalidated.

## M9 — research report and reproducible PDF (2026-08-10 / 08-11)

| Outcome | M9 evidence |
|---|---|
| **D1** | The report states **eight of twelve** research questions as answered within their stated scope and **four as bounded** (RQ1, RQ4, RQ7, RQ11), with RQ4's image-count component explicitly **inconclusive** — a taxonomy corrected at the M9.10 gate after the report's own prose was found to contradict its own table |
| **D2** | Two institutional sources could not be retrieved (HTTP 429 and 403 across five official paths). The search-extract wording drafted from them was **removed rather than kept**, the failure recorded in `docs/evidence/M9/reference-retrieval.md`, and the per-item manifest identified as the real primary evidence |
| **D3** | M9 ran as six human gates — plan, toolchain, visual sample, reference list, conclusions, final PDF — none passed automatically, and each produced recorded corrections |
| **D4** | DR-015 compares six report-build options, states that **only one was measured** and the rest screened, and records what it does not claim: the PDF is reproducible in content but **not byte-identical**, because Chrome embeds a timestamp |
| **D5** | A fact-lock mechanism with graded extraction — structural parsing, full-hash matching, context-anchored patterns that must match exactly once — plus **six negative tests** proving the guards reject a wrong value, a stale value from the wrong column of the right table, an ambiguous anchor, a truncated hash, a missing source and a dead anchor |
| **D6** | The report refuses six conclusions in §19.5; marks requirement 13 **not met**; carries all three CI qualifications; and had a **generalisability overclaim removed** at the M9.10 gate |
| **D7** | A 91-page report built by `python scripts/build_report.py` from tracked Markdown at **zero new dependencies**, validated by `scripts/validate_report.py`, with 20 retrieved-and-read references and every repeated number substituted from proved fact locks |

**The correction that matters most for D6.** The report claimed every rubric score was the student's.
The preserved gate artifacts record **"ChatGPT visual review with Kylian present"** (Gate 1) and
**"Visual-analysis assistance: ChatGPT"** with Kylian as final human approver (Gate 2). The claim was
corrected across nine locations rather than the evidence being reinterpreted to fit it.
