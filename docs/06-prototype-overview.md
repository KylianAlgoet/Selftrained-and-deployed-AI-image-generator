# Prototype overview

**Created:** 2026-07-27 · Each prototype gets a detailed document in `docs/prototypes/` when it starts, containing: research question/hypothesis, scope, acceptance criteria, implementation notes, real tests, actual results, evidence, conclusion, impact on next iteration, related commits. No prototype may be skipped.

| # | Prototype | Research question | Scope sketch | Draft acceptance criteria | Status |
|---|---|---|---|---|---|
| 0 | Static interactive 3D skateboard viewer | RQ9: deck UV mapping, orientation, dynamic textures | React+Vite+TS+R3F scene, self-created procedural deck (DR-005), orbit/zoom/reset, runtime texture swap | Deck renders; rotate/zoom/reset work; test decal maps with correct nose–tail orientation; texture swaps without reload | **completed 2026-07-27** — hypothesis confirmed, 13 tests passing, [results](prototypes/prototype-0.md), [evidence](evidence/prototype-0/) |
| 1 | Local base-model text-to-image benchmark | RQ2, RQ8: which base model fits 8 GB VRAM; aspect-ratio handling | Python 3.11 venv, pinned Diffusers stack, CUDA smoke test, fixed prompt/seed benchmark of candidate base models | CUDA verified; ≥ 2 base models benchmarked with measured VRAM/speed + rubric grid; aspect-ratio test recorded; EXP rows complete | **completed 2026-07-30** — SD 1.5 selected (DR-007); SDXL does not fit 8 GB at native resolution; deck format = direct 1:3 @ 512×1536; EXP-001…005 recorded, [results](prototypes/prototype-1.md), [evidence](evidence/prototype-1/) |
| 2 | Text + reference-image conditioning | RQ6, RQ7: best conditioning method and its controllability | img2img vs. IP-Adapter (ControlNet if relevant) on fixed prompt+reference kit; strength sweeps — **on SD 1.5 per DR-007, with ~5.5 GB VRAM headroom at 512×512** | Reference influence visibly controllable; methods compared on rubric with fixed seeds; strength sweep documented; EXP rows complete | **completed 2026-08-01** — **standard IP-Adapter selected** (DR-008), default scale 0.55, user range 0.40–0.60; img2img retained as a zero-extra-VRAM fallback only, because at 512×1536 it produced all 6 near-copy flags (3 at dHash 0–1); IP-Adapter at the deck format is feasible but memory-critical (7965.5 of 8187.5 MiB); EXP-007…014 recorded, [results](prototypes/prototype-2.md), [evidence](evidence/prototype-2/) |
| 3 | Local LoRA smoke test | RQ1: is local LoRA training viable end-to-end on this machine | Tiny dataset, minimal steps, train → save → load → generate; measure VRAM/duration | Training completes without OOM; LoRA visibly affects output; peak VRAM + duration measured; reproducible via config + seed | **completed 2026-08-04** — **LoRA selected** (DR-009) at rank 8 / alpha 8, UNet attention, tier 0; 13 runs across 8 experiments with zero tier escalations; 300 steps cost 91 s; the combined SD 1.5 + LoRA + IP-Adapter stack fits the deck format by **202.0 MiB of 8 187.5**; "reproducible via config + seed" recorded as **partially met** (risk R14, diagnosed in M6); EXP-016…019 recorded, [results](prototypes/prototype-3.md), [evidence](evidence/prototype-3/) |
| 4 | Style-learning experiments | RQ1, RQ4, RQ5: real style capture; multi-style vs. per-style | Train on the 3 curated styles; vary dataset size/rank/LR; multi-style vs. separate head-to-head | Each style distinguishable in fixed-seed grids; multi-vs-separate comparison scored; best config selected with justification; EXP rows complete | **completed 2026-08-05** at two human gates — **three separate per-style adapters selected** (DR-010) at default weight 0.7: minimal-geometric EXP-027 step 300 **PASS**, ukiyo-e EXP-028 step 600 **PASS**, retro-poster EXP-029 step 300 **PARTIAL PASS**; the multi-style adapter is **viable but not selected**; **RQ4 image count is INCONCLUSIVE** and non-monotonic; 10 of 12 training runs used; EXP-020…033 recorded, [results](prototypes/prototype-4.md), [evidence](evidence/prototype-4/) |
| 5 | Integrated MVP | Primary RQ end-to-end | FastAPI + validation/security, React UI, full generate flow, 3D preview with dynamic texture, download, reproducibility metadata | Full flow works locally: inputs → generation with LoRA + reference → 3D preview correct orientation → download; errors/loading handled; seeds reproduce | **completed 2026-08-07** at the final human visual gate, 12 of 12 manual acceptance items PASS — resident single-flight service, **one API process and one worker by design** (DR-011); real generation progress with no invented percentage (DR-013); **`full-surface`** texture fit selected despite 1.3008× stretch (DR-012); `Upload your own decal` added as a wholly local production feature; worst spare VRAM **200.0 MiB** under real serving; EXP-034/035 recorded, [results](prototypes/prototype-5.md), [evidence](evidence/prototype-5/) |

Statuses update as work happens; completed prototypes link their evidence and commits here.
