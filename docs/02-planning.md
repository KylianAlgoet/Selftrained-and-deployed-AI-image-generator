# Project planning

**Created:** 2026-07-27 · **Deadlines:** feature freeze 2026-08-15 · final content 2026-08-16 18:00 · submission 2026-08-17 06:00 (Europe/Brussels) · presentation 2026-09-02.

Rules: the **original plan (v1) below is never rewritten**. Adjustments are appended in the change log with reasons. Status values: `not started / in progress / done / blocked / dropped`. Effort in focused hours; actuals filled only when true.

## Original plan v1 (2026-07-27)

| # | Milestone | Key tasks | Dates | Priority | Depends on | Evidence | Est. effort | Actual effort | Completed | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| M0 | Phase 0 foundation | Repo inspection, environment audit, architecture matrices + DRs, research plan, planning, risk register, skeleton, security, CLAUDE.md/rules, docs foundation, experiment registry, traceability, validation, atomic commits | Jul 27–28 | critical | — | This repo state + commits + milestone report | 6 h | | | in progress |
| M1 | Prototype 0: 3D skateboard viewer | Deck model sourcing/licence, R3F scene, orbit/zoom/reset, test-texture swap, nose–tail orientation, evidence | Jul 28–30 | critical | M0 | `docs/prototypes/prototype-0.md`, screenshots, commits | 10 h | ~4 h | 2026-07-27 | done |
| M2 | Dataset research + pipeline | Style definitions (3), source/licence audit, manifest schema, collection, validation/hash/dedupe/caption/split scripts, statistics, contact sheets | Jul 30 – Aug 3 | critical | M0 | `docs/04-dataset-methodology.md`, manifests, script tests | 16 h | ~5 h | 2026-07-27 | done |
| M3 | Prototype 1: base-model benchmark | Python 3.11 venv, pinned installs, CUDA smoke test, SD 1.5 vs. SDXL inference benchmark (VRAM/speed/rubric), aspect-ratio test | Aug 2–4 | critical | M0 | EXP records, benchmark grids | 10 h | ~8 h | 2026-07-30 | done |
| M4 | Prototype 2: text + reference conditioning | img2img vs. IP-Adapter (vs. ControlNet) comparison, strength sweeps, fixed-seed grids | Aug 4–6 | critical | M3 | EXP records, comparison grids | 10 h | ~9 h | 2026-08-01 | done |
| M5 | Prototype 3: LoRA smoke test | Minimal LoRA train on tiny set, VRAM/duration measurements, load-and-generate verification, **plus a mandatory combined SD 1.5 + LoRA + IP-Adapter smoke test at 512×1536 (risk R12)** | Aug 6–8 | critical | M2, M3, M4 | EXP-016a/b, EXP-016, EXP-017a/b, EXP-018, EXP-019a/b; `docs/evidence/prototype-3/`; DR-009; combined-stack result **PASS at 202 MiB spare** | 10 h | ~5 h | 2026-08-04 | **complete** |
| M6 | Prototype 4: style-learning experiments | Per-style LoRAs vs. multi-style LoRA, dataset-size/rank/LR variations, rubric evaluation, method comparison conclusions | Aug 8–11 | critical | M5 | EXP records, evaluation grids, RQ1/4/5 conclusions | 14 h | | | not started |
| M7 | Prototype 5: integrated MVP | FastAPI endpoints + validation/security, React UI, 3D texture pipeline, download, reproducibility metadata, error/loading states | Aug 10–13 | critical | M1, M4, M6 | Working MVP, API docs, commits | 18 h | | | not started |
| M8 | Testing, deployment, demo | Pytest/Vitest/Playwright suites, deployment setup + clean-clone test, demo script + backup plan | Aug 13–15 | high | M7 | Test reports, deployment docs | 10 h | ~11 h | 2026-08-09 | **complete** |
| M9 | Research report PDF | Full report per assignment structure, references, PDF build, spelling/link validation | Aug 12–16 | critical | M6 (results) | `deliverables/` report PDF | 14 h | | | not started |
| M10 | Presentation + jury prep | Presentation PDF, speaker notes, timed demo script, backup demo, jury Q&A | Aug 14–16 | critical | M9 | `deliverables/` presentation PDF, `docs/presentation/` | 8 h | | | not started |
| M11 | Final submission audit | Prompt 6 audit: clean clone, secret/large-file scans, E2E, PDF/link/spelling validation, submission checklist | Aug 16 – 17 06:00 | critical | all | Audit results, checklist | 6 h | | | not started |

**Total estimated effort:** ~132 focused hours over 21 days (~6.3 h/day average — feasible but with little slack; see risk R4).

**Public planning link (live since 2026-07-27):** https://github.com/users/KylianAlgoet/projects/1 — a public GitHub Project mirroring milestones M0–M11 as repository issues #1–#12, each with objective, acceptance criteria, planned start/end dates, priority, dependencies, expected evidence, and current status. M0 is closed as completed; M1–M11 are open (Todo). New repository issues are auto-added to the project by workflow.

## Milestone overlap notes

- M2 (dataset) overlaps M3 (benchmark): benchmark needs no custom data.
- M9 (report) starts before M8 finishes: methodology/dataset/early-prototype chapters can be written from completed evidence while later milestones run.
- Aug 16 is reserved exclusively for M11 validation, exports, and submission preparation.

## Change log

| Date | Change | Reason | Impact |
|---|---|---|---|
| 2026-07-27 | M1 completed 2026-07-27 instead of Jul 28–30, actual effort ~4 h vs 10 h estimated | Procedural-geometry approach (DR-005) removed model-sourcing/licensing work; scaffold and scene came together faster than estimated | Buffer of ~1.5 days gained for the critical path (M2 dataset work can start early); estimates for M7 viewer integration can be trimmed |
| 2026-07-27 | M2 started 2026-07-27 (planned Jul 30); dataset style "graffiti/street art" replaced by "ukiyo-e woodblock" (DR-006, user-approved); target set at ~50 items/style | Early start uses the M1 buffer; graffiti photography is generally artist-copyrighted (licensing risk R3), ukiyo-e has large CC0/PD institutional supply | Reduced licensing risk; original style intent preserved in methodology history |
| 2026-07-27 | M2 completed 2026-07-27 (~5 h vs 16 h est). Two approved sources unavailable at collection time: Digital Comic Museum (Cloudflare-gated) and Art Institute of Chicago (image CDN 403). Shares shifted to already-approved Library of Congress and Met sources respectively (conditions A/B) | Programmatic access blocked by anti-bot/WAF measures; no new sources added, staying within the approved registry | 148-item dataset delivered (all styles ≥40); ~2 further days of buffer on the critical path; AIC/DCM noted as reproducibility caveats for the report |
| 2026-07-30 | M3 started 2026-07-30 (planned Aug 2–4), using the M2 buffer. Dataset style `retro-comic` relabelled **`retro-poster`** (caption phrase → "retro silkscreen poster style") as a terminology/schema correction in M3 step 0, before any experiment; ~1 h added to M3 | Kylian's pre-check found the label contradicted the evidence: the material is WPA / Federal Theatre Project silkscreen posters with no halftone, panels, or sequential art. The wrong label had already propagated into the draft benchmark prompt kit, which would have invalidated every later LoRA comparison built on it | Manifest, schema, captions, tests, scripts, evidence, and docs corrected in one atomic commit; verified that only the `style` and `caption` columns changed (IDs, hashes, splits identical); M2 **not** reopened (acceptance criteria intact, issue #3 commented for traceability); DR-006 amended with a dated correction; two dataset findings (framed scans, text-dominated material) recorded for M4 |
| 2026-08-04 | **M5 completed 2026-08-04** (planned Aug 6–8), **~5 h actual vs 10 h estimated** — the largest under-run of the project so far. **LoRA selected as the fine-tuning method (DR-009)**, rank 8 / alpha 8, UNet attention, tier 0. Scope executed as approved: both training geometries in separate processes, automated technical gates only, **no human rubric gate**. One scope reduction was deliberate and pre-agreed: the 512×1536 arm ran **feasibility probes only** (1 and 10 steps), never a long native training run | The under-run has three measured causes, not optimism. **(1)** Training turned out far cheaper than budgeted — 300 steps in 91 s, so the "long run" the estimate assumed never existed. **(2)** No memory tier escalated anywhere, so the entire tier-1…5 contingency went unused; the ladder guard forbids running deeper tiers once tier 0 passes. **(3)** The M3/M4 foundations were directly reusable — `ResourceSampler`, the frozen prompt kit, the pinned-image-encoder workaround and the Phase-1/Phase-2 boundary were all lifted unchanged, so M5 wrote comparatively little new infrastructure. The micro-gating added time rather than saving it, and was worth it: it is what made every longer run start from a measured projection instead of a guess | **~7 days of critical-path buffer now exist** and M5 finished 4 days ahead of its window. **R12 discharged but re-scoped**: the combined stack fits at the deck format by **202 MiB of 8187.5 (2.5 %)**, *less* margin than IP-Adapter alone — so Prototype 5 must treat it as the production path's memory ceiling, not a base to extend. **M6 inherits three open items**: the trigger-token decision (M5 used dataset captions verbatim, deliberately), whether to run a long *native* 512×1536 training run (a separate decision, out of M5 scope), and whether Textual Inversion / DreamBooth get measured comparisons at all — DR-009 selects LoRA on feasibility, and explicitly does **not** claim superiority over methods never run |
| 2026-08-01 | **M4 completed 2026-08-01** (planned Aug 4–6), ~9 h actual vs 10 h estimated in planning v1 and ~7.5–9.5 h in the M4 plan. **Standard IP-Adapter selected** (DR-008), default scale 0.55, user range 0.40–0.60. EXP-008/EXP-009 ran **96 rows each instead of the planned 60**; ControlNet was compared on criteria but **not implemented**, as approved before execution | The extra rows were measurement-driven, not scope creep: the sweep values and the named influence levels are *different numbers*, so without running both in one shared process the clean-process spot checks would have had no counterpart to compare against, and the comparison and multi-seed sheets needed `medium` across the whole condition grid. A usage-limit cutoff also killed the first orchestrator run mid-EXP-009; the 77 partial rows were discarded and the experiment re-run from scratch rather than reconstructed | ~4 days of critical-path buffer preserved; M4 finished 3 days ahead of its planned window. Prototypes 3–5 build on SD 1.5 + IP-Adapter at scale 0.55 and must budget ~1249 MiB above bare SD 1.5. **A combined SD 1.5 + LoRA + IP-Adapter smoke test at 512×1536 is now a mandatory acceptance item (risk R12)** — IP-Adapter alone already uses 7965.5 of 8187.5 MiB there. img2img is retained as a zero-extra-VRAM fallback only, because it produced every near-copy flag in the milestone at the deck geometry (risk R13) |
| 2026-07-30 | **M3 completed 2026-07-30** (planned Aug 2–4), ~8 h actual vs 10 h estimated. SD 1.5 selected as the base model (DR-007); deck format = direct 1:3 at 512×1536. Third candidate SD 2.1 base **blocked** by HTTP 401 gating, so the comparison rests on two measured candidates | Early start used the M1/M2 buffer. The benchmark answered RQ2 and RQ8 with measured data; the blocked candidate was handled by asking rather than substituting, per the approved plan | ~4 further days of buffer on the critical path (M4 can start early). Two-candidate limitation must be stated in the report. Prototype 2 targets SD 1.5 with ~5.5 GB VRAM headroom; Prototypes 3–4 must assume the SD 1.5-class envelope, since the SDXL figures show LoRA at 1024 is not viable on 8 GB |
| 2026-07-30 | M3 scope refined before execution: benchmark split into **Track A** (all candidates at 512×512 for speed/VRAM/reliability) and **Track B** (each candidate at its designed resolution, SDXL at 1024×1024), reported separately; model revisions pinned to immutable Hugging Face commit SHAs; a **mandatory human-review gate** inserted before DR-007, issue closure, and the milestone push; M3 estimate raised 10 h → ~7–9 h realistic / ~13 h worst case | Scoring every model only at 512×512 would unfairly penalise SDXL, which was never designed for it; unpinned `main` revisions cannot support a reproducibility claim; and the qualitative rubric scores and final model choice are the student's own research judgement (D1/D4/D6), not the assistant's | Fairer, more defensible base-model decision; exact reproducibility limited honestly to within-environment repeatability; Claude's autonomy ends at measurements and evidence, with conclusions gated on Kylian's scores and visual approval |
| 2026-08-05 | **M6 Phase A and Phase B completed 2026-08-05** (planned Aug 8-11), **~10 h actual vs ~12.5-13.5 h estimated**, with a real human gate between the phases. **10 of 12 authorised training runs used**; both contingency slots remain. The milestone STOPS at gate 2: no production checkpoint selected, DR-010 left as a draft with no conclusion, M6 not closed, nothing pushed | The plan's first draft contradicted itself - it promised a review gate and then authorised full runs, contingency adjustments and the final validation matrix before Kylian had seen anything. Kylian's plan review forced the Phase A / Phase B split, and it is what made gate 1 a real decision point rather than a formality. Three further corrections came from the same review: an equal-compute image-count experiment (without it, RQ4 could show that 36-44 images worked but never the EFFECT of count), four explicit caption verdict rules replacing an unfalsifiable hypothesis, and exposure-matched multi-style sampling | **~4 days of critical-path buffer preserved** and M7 can still begin on time. **R12 unchanged at 202.0 MiB spare** - measured identically for all four production candidates, so a higher rank or a second adapter is what is at risk, not this stack. Two limitations carried into the report rather than fixed late: training runs are **not bit-reproducible** from their recorded seed because the LoRA initialisation draws from an unseeded global RNG, and the RQ4 size result is **O5 inconclusive** with a non-monotonic ordering, establishing no minimum image count |
| 2026-08-05 | **M6 COMPLETE 2026-08-05** (planned Aug 8-11), closed at gate 2 by Kylian. **Production selection: three separate per-style LoRA adapters** - minimal-geometric EXP-027 step 300, ukiyo-e EXP-028 step 600, retro-poster EXP-029 step 300 - at a **default weight of 0.7**. **DR-010 finalised.** minimal-geometric and ukiyo-e PASS; **retro-poster PARTIAL PASS** | Two of the three selected checkpoints are step **300**, not the 600 the runs trained to: prompt adherence fell from 4 to 3 at step 600 for both while style consistency stayed at 5. Only ukiyo-e improved with the longer run. That is the concrete payoff of checkpointing at 150/300/450/600 rather than assuming one global step count, and of deciding the count per style at a human gate | **M6 closed 6 days ahead of its planned window and 4 days before the 2026-08-09 hard stop; M7 can begin on schedule.** Prototype 5 inherits three adapters, a 0.7 default with an optional 0.4-1.0 range, a **binding 202 MiB memory ceiling** on the 512x1536 combined path, and **retro-poster as a named partial pass** that must ship with its pseudo-text and framing limitation stated rather than as an equal. **Both contingency slots went unused (10 of 12 runs).** The GitHub issue and board remain Kylian's to update - `gh` is unavailable |
| 2026-08-06 | **M7 (Prototype 5) built and validated 2026-08-06** (planned Aug 10-13), **~9 h actual vs 18 h estimated**, using the buffer M6 earned by finishing early. Scope executed as approved, plus **one addition**: a review-only "Load decal" control in the viewer. **STOPPED at the human review gate** - no texture-fit default chosen, M7 not declared complete, nothing pushed | The under-run has two measured causes. **(1)** M3-M6 infrastructure was directly reusable: the API calls `attach_ip_adapter` with its pinned-encoder workaround, the frozen prompt kit and the `cross_attention_kwargs` scale mechanism unchanged, so the service adds no modelling of its own and inherits comparability with EXP-031 for free. **(2)** EXP-034 passed on the first run, so none of the contingency for a residency failure was needed. The "Load decal" control was **not** scope creep: the milestone declared a hard cap of 25 generations, the cap was reached exactly, and the gate requires side-by-side fit screenshots - loading an existing decal was the only way to produce them without a 26th generation, and Prototype 0's bundled decals are 512x2000 so they cannot show a 1:3 mismatch | **The 202 MiB ceiling is now 200.0 MiB.** EXP-034 measured the worst spare in real serving conditions as **200.0 MiB**, tighter than EXP-032's figure, and that is the operative production number for M8 onward. Residency itself is free: allocated memory after generation was identical across all 13 runs and peak allocated matched M5's EXP-019b byte for byte. **A geometry decision is now pending Kylian**: generated decals are 1:3 and the deck UV domain is 1:3.902, so full-surface stretches by 1.3008x and fit-without-stretch leaves 23.12% bare. Both are built; neither is selected. **M8 inherits a single-process service by design** - the busy lock is process-local and a second pipeline does not fit, so scaling is not a configuration change |
| 2026-08-07 | **M7 review gate: the texture-fit decision returned, M7 still OPEN.** Kylian selected **`full-surface`** as the production default (**DR-012**); implemented as `DEFAULT_TEXTURE_FIT_MODE`, with the rejected mode kept selectable. ~0.5 h actual, unestimated - the gate was a wait state, not a planned work item. **M7 was NOT closed**: asked directly, he answered "Not yet - I'll walk the checklist", so the 12-item manual acceptance checklist stands unwalked | The gate resolved in one exchange because the handover had already reduced the question to two screenshots of the same decal at the same camera with one variable changed. **The reason was asked for rather than written**: a decision record whose justification the assistant invented would be worse evidence than one with no justification, so his wording is quoted verbatim in DR-012. The pre-existing test asserting that **no** default was exported is what made this a recorded decision instead of a silent code change - it had to be deliberately replaced | **No GPU budget was spent** - the cap stayed at 25 of 25 and the gate reused the existing screenshots. Validation after the change: **371 pytest** (unchanged), **70 vitest** (five added, one removed), eslint clean, build succeeds. **M7 remains in progress**, nothing is pushed, the issue and board are untouched, and M8 has not begun. The remaining path to closure is Kylian's checklist walk, then the milestone report |
| 2026-08-07 | **M7 functional walkthrough PASSED; one interface pass added, M7 still OPEN.** Kylian walked the full workflow - prompt-only, deterministic repeat, reference-conditioned, style switching, 2D result, 3D application, camera, downloads, metadata, retro-poster warnings, invalid uploads, UV orientation - and it passed. He then scoped one final UI/UX pass plus real generation progress, ~3 h actual, unestimated (not in the original M7 plan). **406 pytest / 152 vitest.** Stops at a **visual review gate** | The pass was scoped by his constraints, not by taste: no new dependency, no SSE/WebSocket/job queue/second worker, a strict non-regression list, and one explicit rule - **"this must not be a fake progress bar"**. That rule is what produced DR-013's design: only denoising has a real denominator, so model loading, decoding and saving get a stage name and no number, and no weighted overall percentage exists. The static "around 15 seconds" line it replaced was wrong for a cold start by a factor of two | **No GPU budget spent** - the cap stands at 25 of 25, generation was intercepted in the browser and the API reported `pipeline_loaded: false` afterwards. **One qualitative finding preserved, not fixed:** a futuristic-skyline prompt produced a clearly minimal-geometric, usable deck graphic in which the skyline and skateboarder were not clearly represented - strong style conditioning dominating detailed content. It is a known prompt-adherence limitation consistent with M6, **not** a frontend failure, and nothing was retrained or rewritten in response. **Three self-inflicted defects found and fixed**, none of them visible to any test: a 70 px desktop viewport overflow, a page-wide horizontal scrollbar from a CSS specificity conflict, and a poll loop restarting on every render |
| 2026-08-07 | **M7 visual review passed with two follow-ups; M7 still OPEN.** Kylian reviewed the interface live and passed it, then asked for two things: verification from real telemetry of what his generation displayed, and **local decal upload as a normal production feature**. Both delivered, ~1.5 h, unestimated. **406 pytest / 165 vitest** | The upload is a product decision, not a technical one: users who already own artwork should not have to spend a generation to see it on a board. Keeping it **wholly local** - decoded in the browser, never sent, never touching the model - is what makes it free to offer, and keeping it visually separate from the AI reference upload is what stops the two being confused | **The GPU cap of 25 was exceeded: Kylian ran a 26th generation during his review.** His decision, recorded rather than absorbed. Its telemetry confirmed real step tracking (30/30 across 48 polls) but **cannot** confirm what the browser painted - the server logs requests, not rendered text - and that limit is stated rather than smoothed over. No defect found, so nothing was fixed; `Finalising the decal…` is genuinely ~1 s and **will not be padded** |
| 2026-08-07 | **M7 COMPLETE 2026-08-07** (planned Aug 10-13), closed at the final human visual gate by Kylian: **12 of 12 manual acceptance items PASS**. Approved: the redesigned production interface, the real progress/ETA implementation, `Upload your own decal` as a production feature, and `full-surface` re-confirmed. **~13 h actual vs 18 h estimated.** Closed **locally only** - no push, no remote issue change, no board move | M7 ran in three passes rather than one: build and measure (Aug 6), a texture-fit decision gate, then a professional interface pass Kylian scoped after using the application. The third pass was not in the plan and was worth the time - it produced DR-013 and found three defects no test could see. The pattern that made all three work is the same one M6 established: **build both options, decide nothing, and stop at a human gate** | **The GPU budget closed at 26 total, not 25.** The research budget finished at 25/25; generation 26 was Kylian's own manual review run, **outside the frozen research matrix** and deliberately **not** registered as an experiment. **M8 inherits:** a single-process service by design, a **~200 MiB** ceiling, `full-surface`, `retro-poster` as a named partial pass, and an accepted **prompt-adherence limitation**. Six limitations were approved *as limitations* rather than deferred as defects. **M8 has not begun**; the push, the GitHub issue and the project board remain Kylian's |
| 2026-08-09 | **M8 COMPLETE 2026-08-09** (planned Aug 13-15), **~11 h actual vs 10 h estimated**, run four days early on the buffer M7 earned. All six acceptance criteria met. Added: **37 Playwright E2E scenarios** (the layer M7 explicitly did not have), +67 pytest, +4 vitest, **DR-014**, a deployment runbook and weights manifest, four PowerShell scripts, a non-GPU CI workflow, a real clean-clone test, a timed demo script and a backup demo plan. **Feature freeze in force** | **M8 was scoped to verify, and it discovered.** Five real defects surfaced, none of which any existing suite could have caught, because every one was invisible on the machine the code was written on: a **frozen dataset hash that failed on every clean clone** (it hashed a CRLF working copy while Git stores LF, so an integrity control had only ever passed locally); `.env.example` documenting five settings nothing reads, two implying upload rules were configurable; a **stale audited Node version** (20.18.0 recorded, 24.18.0 actual); a requirements file claiming a pin it did not make; and `uvicorn --workers 1` starting two processes, so a naive stop strands the worker on port 8000. The planned CI skip-marker for checkpoint tests turned out to be **unnecessary** - checking showed the suite already passes with the weights removed, so none was added | **The clean clone reproduced M7's Phase A output byte-for-byte** - sha256 `46bbf160e427...`, 1 089 939 bytes, fresh environment, three days later. Inference is deterministic and portable given a fixed adapter, which **does not contradict R14** (that concerns *training*). `peak_allocated_mb` 5143.73 is byte-identical across EXP-019b, EXP-034 and this run. **Generation total is 27**, not 25: 25 research + 1 M7 review + 1 M8 deployment validation, the last carrying no EXP-### and absent from the registry. Suites: **473 pytest / 169 vitest / 37 E2E**, eslint clean, build succeeds. The CI workflow is committed but **has never run** - nothing was pushed |
| 2026-08-11 | **M9 (research report) authored 2026-08-10/11** (planned Aug 12-16), all **26 mandated sections** written, built to a **90-page PDF** at zero new dependencies. Six human gates, none passed automatically. **Three corrections were required at the conclusions gate**, two of which corrected claims the report was making | The mandated structure is **26 sections, not the 24** the milestone briefing assumed - enumerated from Prompt 5 and verified. The report's AI-scoring disclosure **overstated the human/AI separation**: the preserved Gate-1 and Gate-2 artifacts record ChatGPT contributing visual analysis and proposed scoring with Kylian as final approver, and the wording was corrected in nine places rather than the evidence being reread to fit it. The RQ status count contradicted the report's own table and is now a consistent taxonomy: eight answered within scope, four bounded, RQ4's image-count component inconclusive | **90 pages accepted** rather than compressed: a genuine repetition pass moved the count by under one page, identifying the cause as structural (26 sections each starting a new page cost ~13). **Two institutional references could not be retrieved** - HTTP 429 and 403 across five official paths - so the extract-based wording was removed and the failure recorded; this is the **fourth** time third-party hosting has obstructed the project. Gaps closed on the way: `prototype-3.md` did not exist, the prototype overview listed three completed prototypes as not started, and the README carried stale suite counts. **M10 has not begun** |
| 2026-08-14 | **M10 (presentation) built, NOT closed** (planned Aug 16-17; brought forward). 26 slides authored 2026-08-11, rebuilt and documented 2026-08-14; **DR-016** written; both PDFs tracked. ~3 h actual on 08-14 against no separate estimate — M10 was estimated as one block with the defence preparation. **Stops before the human visual gate**: no judgement about the deck's content has been made | **The deck existed for three days as an undocumented, unverified artifact.** The 08-11 session authored all 26 slides and built the PDFs but wrote no decision record, no process-log entry and made no commit, while four files cited a "DR-016" that did not exist. Resuming, the first action was to verify rather than trust the artifact — which is what found that **the committed PDF did not match its sources**: it was built at 05:00 and the CSS and one slide were edited at 05:03, leaving **216 006 bytes, about 20 %, missing** from the deliverable with nothing in the repository able to report it. The pipeline extends DR-015 instead of duplicating it, and the deck holds **no facts of its own**, so the report's fact locks govern both | **Writing DR-016 changed the report.** `decision_record_count` globs `docs/decisions/`, so the record moved it 15 → 16, **failed the deck build**, and required Appendix B and a rebuild — which took the report **90 → 91 pages** and invalidated the typed "90-page report" on slide 26 and in the traceability matrix. The report PDF is superseded: **2 769 385 bytes, `73f57415…`, 91 pages**. A page count **cannot** be fact-locked with the current extractors, so it stands as a live drift risk in DR-016. **Two things remain unvalidated and both are Kylian's:** no authoritative presentation duration is recorded anywhere in this repository, so the 26-slide length is provisional; and **no rehearsal has been run**, so ~30 minutes is an estimate from note word counts at 130 wpm, never a measured time. Issue #10 still open, nothing pushed |
| 2026-08-14 | **M10 restructured the same day: 26 slides → 15.** Kylian supplied the authoritative slot — **20:00 in total, including the live demo** — which had been the milestone's one open blocker. The 26-slide deck estimated **26.4 min** of narration and **did not fit, by roughly 50 %**. **DR-017** written; every speaker note rewritten against a per-slide target; `jury-questions.md` created for what came off the visible slides. ~2 h actual, unestimated — the restructuring was not a planned work item | **Every structural check had passed on a deck that did not fit**: 26 pages for 26 slides, 0 hard failures, 0 advisories, all fact locks resolving. The checks were not wrong — **the binding constraint was absent from the repository, so nothing could fail on it.** A validator enforces only the requirements it has been told, and this one had been searched for twice and recorded as an open item. The corollary is that DR-016's `core`/`supporting` tiering was **inadequate on its own**: designed for not knowing the duration, it made only 4 of 26 slides cuttable, saving ~4 min against an ~11 min overrun. **Tiering absorbs variation; it does not absorb being wrong about the budget** | Timing is now a **hard build check** with the budget declared in `deck.yaml`, and the demo is **never counted as zero** — the spoken handoff and the demo itself are declared costs. Layout budgets **tightened** (bullets 900 → 580 visible characters) so one central claim per slide is enforced rather than trusted. **Nothing was deleted from the project**: everything cut moved into speaker notes or `docs/presentation/jury-questions.md`. A second guard was added after a rendering defect the budgets could not see — the `limits` slide used two-column markup under a `bullets` layout, which is valid HTML, passes every budget, and renders wrong. **Measured: narration 14:56 · demo 4:00 · combined 18:56 · buffer 1:04 · 523 pytest — all timing figures ESTIMATES at 130 wpm, no rehearsal run.** M10 criterion C stays OPEN; nothing pushed |
| 2026-08-14 | **M10 content review: corrections required, milestone NOT complete.** Kylian reviewed the 15-slide deck and approved the structure while rejecting two claims on slide 3 and the size of the timing buffer. ~1.5 h actual, unestimated. **The 15-slide structure is approved; the content is not yet gated** | **Two false claims that every automated check had passed.** The deck stated the card total, the stack's peak *allocated* and the spare as if the first two produced the third — `8187.5 − 5143.73 = 3043.77`, not 200 — because peak allocated counts live tensors while the margin is measured against total device occupancy (**device used 7987.5**, which subtracts exactly). **Every number was individually true and correctly fact-locked; the relationship implied between them was false**, which a fact-lock system structurally cannot see. And the deck claimed both human gates were blinded, while `GATE-2-approval.md` records the opposite in its own words — Gate 2's sheets were **labelled**, with a stated expectation effect. **The 26-slide deck had this right**; the error was introduced by the merge, which is precisely the compression risk the slide validator exists to police | `worst_device_used_mib` added as a fact lock so the slide's arithmetic is checkable rather than plausible. Timing band **tightened** 870–900 s → **825–855 s** with a **1:45** minimum buffer, and the extra minute taken out of **notes whose reasoning jury-questions.md already carries** — not out of a slide, and **not out of a faster assumed rate** (130 wpm unchanged). **This is the one case where moving a bound was right: it was tightened as a safety requirement and the deck cut to meet it, not loosened to admit failing work.** **Measured: narration 14:11 · demo 4:00 · combined 18:11 · buffer 1:49 · 32 fact locks · 525 pytest.** **M10 is NOT complete** — visual gate OPEN, rehearsal never run, criterion C OPEN, nothing pushed |
| 2026-08-15 | **M11 (final submission audit) executed** (planned Aug 15-17). The thirteen mandatory requirements in `docs/00-project-brief.md` checked one by one against evidence that was opened, run or measured — never inferred from a feature existing. Nine B-checks plus a full clean-clone run: clone → documented restore → running backend and frontend, outside the working repository. ~4 h actual against an 8 h estimate for the block | **11 PASS, 2 PARTIAL, and neither PARTIAL is a defect in the work** — requirement 12 needs a `git push` (19 commits ahead, 0 behind) and requirement 13 needs a human to look at the deck and speak it against a clock. The audit's value was in the two things it found that no existing check could: **M10's unexplained 800 KB short report build reproduced**, and this time the artifact was **copied aside before rebuilding** — the step M10 could not take. **Direct comparison disproved the risk as it had been carried.** Text-drawing operations 19 434 in both builds with a byte-identical ordered hash, same 6 images, same 9 fonts, same 7 032 objects, same 91 pages. All 801 147 bytes are 1×1 rectangles filled `#c9cfd4`: Chrome rasterising the dotted border of `.toc__dots` into individual fills, replicated across all 91 page streams because its paged output puts the whole document in every page and clips a window into it. **The feared failure — a report shipping with missing figures and looking correct — cannot be what was observed.** The defect is cosmetic: the table-of-contents dot leaders fail to render | **A detector was written, not a gate.** `docs/evidence/M11/check_report_leaders.py` reports on any PDF and was verified against both real artifacts (PASS 1 500 559 fills, FAIL 34 729); it stays outside `build_report.py`, because **gating** the build edits it under a freeze for a cosmetic defect and that is Kylian's call. **Second finding: the clean clone printed 522 passed / 5 skipped while two documents still said 468 / 5 in the present tense.** The arithmetic was never inconsistent — chapter 15 is scoped to the 473 system tests — but the sentence describes what a command prints, and that moved when M9/M10 added 54 document-validation tests. Corrected in `README.md` and `report/sources/15-testing.md` and the report rebuilt; **deliberately left in `docs/07-testing-strategy.md`, where it sits under a heading marked historical and is correct for its date.** **Measured: 527 pytest · 522+5 clean clone · 183 vitest · 38 E2E · 34 fact locks · report 91 pages, 2 773 114 B.** **Requirements 12 and 13 stay OPEN and are Kylian's** |
