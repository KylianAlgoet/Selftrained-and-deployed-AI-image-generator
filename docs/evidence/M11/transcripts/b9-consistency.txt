=== B9 CONSISTENCY AUDIT - 2026-08-15T01:51:03Z ===
MEASURED THIS SESSION (authoritative): pytest 527 | vitest 183 | Playwright 38

--- every pytest count claim outside the process log and evidence archive ---
README.md:72:.venv\Scripts\python.exe -m pytest    # 473 — API + ML tooling, pipeline stubbed, no GPU
README.md:80:A clean clone reports **468 passed / 5 skipped** for pytest: five tests skip conditionally on
docs/02-planning.md:48:| 2026-08-06 | **M7 (Prototype 5) built and validated 2026-08-06** (planned Aug 10-13), **~9 h actual vs 18 h estimated**, using the buffer M6 earned by finishing early. Scope executed as approved, plus **one addition**: a review-only "Load decal" control in the viewer. **STOPPED at the human review gate** - no texture-fit default chosen, M7 not declared complete, nothing pushed | The under-run has two measured causes. **(1)** M3-M6 infrastructure was directly reusable: the API calls `attach_ip_adapter` with its pinned-encoder workaround, the frozen prompt kit and the `cross_attention_kwargs` scale mechanism unchanged, so the service adds no modelling of its own and inherits comparability with EXP-031 for free. **(2)** EXP-034 passed on the first run, so none of the contingency for a residency failure was needed. The "Load decal" control was **not** scope creep: the milestone declared a hard cap of 25 generations, the cap was reached exactly, and the gate requires side-by-side fit screenshots - loading an existing decal was the only way to produce them without a 26th generation, and Prototype 0's bundled decals are 512x2000 so they cannot show a 1:3 mismatch | **The 202 MiB ceiling is now 200.0 MiB.** EXP-034 measured the worst spare in real serving conditions as **200.0 MiB**, tighter than EXP-032's figure, and that is the operative production number for M8 onward. Residency itself is free: allocated memory after generation was identical across all 13 runs and peak allocated matched M5's EXP-019b byte for byte. **A geometry decision is now pending Kylian**: generated decals are 1:3 and the deck UV domain is 1:3.902, so full-surface stretches by 1.3008x and fit-without-stretch leaves 23.12% bare. Both are built; neither is selected. **M8 inherits a single-process service by design** - the busy lock is process-local and a second pipeline does not fit, so scaling is not a configuration change |
docs/02-planning.md:49:| 2026-08-07 | **M7 review gate: the texture-fit decision returned, M7 still OPEN.** Kylian selected **`full-surface`** as the production default (**DR-012**); implemented as `DEFAULT_TEXTURE_FIT_MODE`, with the rejected mode kept selectable. ~0.5 h actual, unestimated - the gate was a wait state, not a planned work item. **M7 was NOT closed**: asked directly, he answered "Not yet - I'll walk the checklist", so the 12-item manual acceptance checklist stands unwalked | The gate resolved in one exchange because the handover had already reduced the question to two screenshots of the same decal at the same camera with one variable changed. **The reason was asked for rather than written**: a decision record whose justification the assistant invented would be worse evidence than one with no justification, so his wording is quoted verbatim in DR-012. The pre-existing test asserting that **no** default was exported is what made this a recorded decision instead of a silent code change - it had to be deliberately replaced | **No GPU budget was spent** - the cap stayed at 25 of 25 and the gate reused the existing screenshots. Validation after the change: **371 pytest** (unchanged), **70 vitest** (five added, one removed), eslint clean, build succeeds. **M7 remains in progress**, nothing is pushed, the issue and board are untouched, and M8 has not begun. The remaining path to closure is Kylian's checklist walk, then the milestone report |
docs/02-planning.md:50:| 2026-08-07 | **M7 functional walkthrough PASSED; one interface pass added, M7 still OPEN.** Kylian walked the full workflow - prompt-only, deterministic repeat, reference-conditioned, style switching, 2D result, 3D application, camera, downloads, metadata, retro-poster warnings, invalid uploads, UV orientation - and it passed. He then scoped one final UI/UX pass plus real generation progress, ~3 h actual, unestimated (not in the original M7 plan). **406 pytest / 152 vitest.** Stops at a **visual review gate** | The pass was scoped by his constraints, not by taste: no new dependency, no SSE/WebSocket/job queue/second worker, a strict non-regression list, and one explicit rule - **"this must not be a fake progress bar"**. That rule is what produced DR-013's design: only denoising has a real denominator, so model loading, decoding and saving get a stage name and no number, and no weighted overall percentage exists. The static "around 15 seconds" line it replaced was wrong for a cold start by a factor of two | **No GPU budget spent** - the cap stands at 25 of 25, generation was intercepted in the browser and the API reported `pipeline_loaded: false` afterwards. **One qualitative finding preserved, not fixed:** a futuristic-skyline prompt produced a clearly minimal-geometric, usable deck graphic in which the skyline and skateboarder were not clearly represented - strong style conditioning dominating detailed content. It is a known prompt-adherence limitation consistent with M6, **not** a frontend failure, and nothing was retrained or rewritten in response. **Three self-inflicted defects found and fixed**, none of them visible to any test: a 70 px desktop viewport overflow, a page-wide horizontal scrollbar from a CSS specificity conflict, and a poll loop restarting on every render |
docs/02-planning.md:51:| 2026-08-07 | **M7 visual review passed with two follow-ups; M7 still OPEN.** Kylian reviewed the interface live and passed it, then asked for two things: verification from real telemetry of what his generation displayed, and **local decal upload as a normal production feature**. Both delivered, ~1.5 h, unestimated. **406 pytest / 165 vitest** | The upload is a product decision, not a technical one: users who already own artwork should not have to spend a generation to see it on a board. Keeping it **wholly local** - decoded in the browser, never sent, never touching the model - is what makes it free to offer, and keeping it visually separate from the AI reference upload is what stops the two being confused | **The GPU cap of 25 was exceeded: Kylian ran a 26th generation during his review.** His decision, recorded rather than absorbed. Its telemetry confirmed real step tracking (30/30 across 48 polls) but **cannot** confirm what the browser painted - the server logs requests, not rendered text - and that limit is stated rather than smoothed over. No defect found, so nothing was fixed; `Finalising the decal…` is genuinely ~1 s and **will not be padded** |
docs/02-planning.md:53:| 2026-08-09 | **M8 COMPLETE 2026-08-09** (planned Aug 13-15), **~11 h actual vs 10 h estimated**, run four days early on the buffer M7 earned. All six acceptance criteria met. Added: **37 Playwright E2E scenarios** (the layer M7 explicitly did not have), +67 pytest, +4 vitest, **DR-014**, a deployment runbook and weights manifest, four PowerShell scripts, a non-GPU CI workflow, a real clean-clone test, a timed demo script and a backup demo plan. **Feature freeze in force** | **M8 was scoped to verify, and it discovered.** Five real defects surfaced, none of which any existing suite could have caught, because every one was invisible on the machine the code was written on: a **frozen dataset hash that failed on every clean clone** (it hashed a CRLF working copy while Git stores LF, so an integrity control had only ever passed locally); `.env.example` documenting five settings nothing reads, two implying upload rules were configurable; a **stale audited Node version** (20.18.0 recorded, 24.18.0 actual); a requirements file claiming a pin it did not make; and `uvicorn --workers 1` starting two processes, so a naive stop strands the worker on port 8000. The planned CI skip-marker for checkpoint tests turned out to be **unnecessary** - checking showed the suite already passes with the weights removed, so none was added | **The clean clone reproduced M7's Phase A output byte-for-byte** - sha256 `46bbf160e427...`, 1 089 939 bytes, fresh environment, three days later. Inference is deterministic and portable given a fixed adapter, which **does not contradict R14** (that concerns *training*). `peak_allocated_mb` 5143.73 is byte-identical across EXP-019b, EXP-034 and this run. **Generation total is 27**, not 25: 25 research + 1 M7 review + 1 M8 deployment validation, the last carrying no EXP-### and absent from the registry. Suites: **473 pytest / 169 vitest / 37 E2E**, eslint clean, build succeeds. The CI workflow is committed but **has never run** - nothing was pushed |
docs/02-planning.md:56:| 2026-08-14 | **M10 restructured the same day: 26 slides → 15.** Kylian supplied the authoritative slot — **20:00 in total, including the live demo** — which had been the milestone's one open blocker. The 26-slide deck estimated **26.4 min** of narration and **did not fit, by roughly 50 %**. **DR-017** written; every speaker note rewritten against a per-slide target; `jury-questions.md` created for what came off the visible slides. ~2 h actual, unestimated — the restructuring was not a planned work item | **Every structural check had passed on a deck that did not fit**: 26 pages for 26 slides, 0 hard failures, 0 advisories, all fact locks resolving. The checks were not wrong — **the binding constraint was absent from the repository, so nothing could fail on it.** A validator enforces only the requirements it has been told, and this one had been searched for twice and recorded as an open item. The corollary is that DR-016's `core`/`supporting` tiering was **inadequate on its own**: designed for not knowing the duration, it made only 4 of 26 slides cuttable, saving ~4 min against an ~11 min overrun. **Tiering absorbs variation; it does not absorb being wrong about the budget** | Timing is now a **hard build check** with the budget declared in `deck.yaml`, and the demo is **never counted as zero** — the spoken handoff and the demo itself are declared costs. Layout budgets **tightened** (bullets 900 → 580 visible characters) so one central claim per slide is enforced rather than trusted. **Nothing was deleted from the project**: everything cut moved into speaker notes or `docs/presentation/jury-questions.md`. A second guard was added after a rendering defect the budgets could not see — the `limits` slide used two-column markup under a `bullets` layout, which is valid HTML, passes every budget, and renders wrong. **Measured: narration 14:56 · demo 4:00 · combined 18:56 · buffer 1:04 · 523 pytest — all timing figures ESTIMATES at 130 wpm, no rehearsal run.** M10 criterion C stays OPEN; nothing pushed |
docs/02-planning.md:57:| 2026-08-14 | **M10 content review: corrections required, milestone NOT complete.** Kylian reviewed the 15-slide deck and approved the structure while rejecting two claims on slide 3 and the size of the timing buffer. ~1.5 h actual, unestimated. **The 15-slide structure is approved; the content is not yet gated** | **Two false claims that every automated check had passed.** The deck stated the card total, the stack's peak *allocated* and the spare as if the first two produced the third — `8187.5 − 5143.73 = 3043.77`, not 200 — because peak allocated counts live tensors while the margin is measured against total device occupancy (**device used 7987.5**, which subtracts exactly). **Every number was individually true and correctly fact-locked; the relationship implied between them was false**, which a fact-lock system structurally cannot see. And the deck claimed both human gates were blinded, while `GATE-2-approval.md` records the opposite in its own words — Gate 2's sheets were **labelled**, with a stated expectation effect. **The 26-slide deck had this right**; the error was introduced by the merge, which is precisely the compression risk the slide validator exists to police | `worst_device_used_mib` added as a fact lock so the slide's arithmetic is checkable rather than plausible. Timing band **tightened** 870–900 s → **825–855 s** with a **1:45** minimum buffer, and the extra minute taken out of **notes whose reasoning jury-questions.md already carries** — not out of a slide, and **not out of a faster assumed rate** (130 wpm unchanged). **This is the one case where moving a bound was right: it was tightened as a safety requirement and the deck cut to meet it, not loosened to admit failing work.** **Measured: narration 14:11 · demo 4:00 · combined 18:11 · buffer 1:49 · 32 fact locks · 525 pytest.** **M10 is NOT complete** — visual gate OPEN, rehearsal never run, criterion C OPEN, nothing pushed |
docs/07-testing-strategy.md:3:**Created:** 2026-07-27 · **Updated:** 2026-08-07 (M7 closure) · **Status:** **406 pytest tests and 165 vitest tests exist and pass.** The API and frontend-unit layers arrived with M7; the E2E (Playwright) layer is still outstanding and belongs to M8.
docs/07-testing-strategy.md:26:Growth: M7 closed at **406** pytest / **165** vitest / **0** E2E. M8 added +35 upload-security,
docs/07-testing-strategy.md:30:**A clean clone runs 468 passed / 5 skipped.** The five are pre-existing conditional skips for
docs/deployment/runbook.md:127:.\.venv\Scripts\python.exe -m pytest        # 461, no GPU, pipeline stubbed
report/sources/15-testing.md:16:**A clean clone runs 468 passed and 5 skipped.** The five are pre-existing conditional skips for
report/sources/15-testing.md:96:The suites grew from 406 pytest and 165 vitest at the MVP's close to

--- vitest / playwright claims ---
docs/02-planning.md:49:| 2026-08-07 | **M7 review gate: the texture-fit decision returned, M7 still OPEN.** Kylian selected **`full-surface`** as the production default (**DR-012**); implemented as `DEFAULT_TEXTURE_FIT_MODE`, with the rejected mode kept selectable. ~0.5 h actual, unestimated - the gate was a wait state, not a planned work item. **M7 was NOT closed**: asked directly, he answered "Not yet - I'll walk the checklist", so the 12-item manual acceptance checklist stands unwalked | The gate resolved in one exchange because the handover had already reduced the question to two screenshots of the same decal at the same camera with one variable changed. **The reason was asked for rather than written**: a decision record whose justification the assistant invented would be worse evidence than one with no justification, so his wording is quoted verbatim in DR-012. The pre-existing test asserting that **no** default was exported is what made this a recorded decision instead of a silent code change - it had to be deliberately replaced | **No GPU budget was spent** - the cap stayed at 25 of 25 and the gate reused the existing screenshots. Validation after the change: **371 pytest** (unchanged), **70 vitest** (five added, one removed), eslint clean, build succeeds. **M7 remains in progress**, nothing is pushed, the issue and board are untouched, and M8 has not begun. The remaining path to closure is Kylian's checklist walk, then the milestone report |
docs/02-planning.md:50:| 2026-08-07 | **M7 functional walkthrough PASSED; one interface pass added, M7 still OPEN.** Kylian walked the full workflow - prompt-only, deterministic repeat, reference-conditioned, style switching, 2D result, 3D application, camera, downloads, metadata, retro-poster warnings, invalid uploads, UV orientation - and it passed. He then scoped one final UI/UX pass plus real generation progress, ~3 h actual, unestimated (not in the original M7 plan). **406 pytest / 152 vitest.** Stops at a **visual review gate** | The pass was scoped by his constraints, not by taste: no new dependency, no SSE/WebSocket/job queue/second worker, a strict non-regression list, and one explicit rule - **"this must not be a fake progress bar"**. That rule is what produced DR-013's design: only denoising has a real denominator, so model loading, decoding and saving get a stage name and no number, and no weighted overall percentage exists. The static "around 15 seconds" line it replaced was wrong for a cold start by a factor of two | **No GPU budget spent** - the cap stands at 25 of 25, generation was intercepted in the browser and the API reported `pipeline_loaded: false` afterwards. **One qualitative finding preserved, not fixed:** a futuristic-skyline prompt produced a clearly minimal-geometric, usable deck graphic in which the skyline and skateboarder were not clearly represented - strong style conditioning dominating detailed content. It is a known prompt-adherence limitation consistent with M6, **not** a frontend failure, and nothing was retrained or rewritten in response. **Three self-inflicted defects found and fixed**, none of them visible to any test: a 70 px desktop viewport overflow, a page-wide horizontal scrollbar from a CSS specificity conflict, and a poll loop restarting on every render |
docs/02-planning.md:51:| 2026-08-07 | **M7 visual review passed with two follow-ups; M7 still OPEN.** Kylian reviewed the interface live and passed it, then asked for two things: verification from real telemetry of what his generation displayed, and **local decal upload as a normal production feature**. Both delivered, ~1.5 h, unestimated. **406 pytest / 165 vitest** | The upload is a product decision, not a technical one: users who already own artwork should not have to spend a generation to see it on a board. Keeping it **wholly local** - decoded in the browser, never sent, never touching the model - is what makes it free to offer, and keeping it visually separate from the AI reference upload is what stops the two being confused | **The GPU cap of 25 was exceeded: Kylian ran a 26th generation during his review.** His decision, recorded rather than absorbed. Its telemetry confirmed real step tracking (30/30 across 48 polls) but **cannot** confirm what the browser painted - the server logs requests, not rendered text - and that limit is stated rather than smoothed over. No defect found, so nothing was fixed; `Finalising the decal…` is genuinely ~1 s and **will not be padded** |
docs/02-planning.md:53:| 2026-08-09 | **M8 COMPLETE 2026-08-09** (planned Aug 13-15), **~11 h actual vs 10 h estimated**, run four days early on the buffer M7 earned. All six acceptance criteria met. Added: **37 Playwright E2E scenarios** (the layer M7 explicitly did not have), +67 pytest, +4 vitest, **DR-014**, a deployment runbook and weights manifest, four PowerShell scripts, a non-GPU CI workflow, a real clean-clone test, a timed demo script and a backup demo plan. **Feature freeze in force** | **M8 was scoped to verify, and it discovered.** Five real defects surfaced, none of which any existing suite could have caught, because every one was invisible on the machine the code was written on: a **frozen dataset hash that failed on every clean clone** (it hashed a CRLF working copy while Git stores LF, so an integrity control had only ever passed locally); `.env.example` documenting five settings nothing reads, two implying upload rules were configurable; a **stale audited Node version** (20.18.0 recorded, 24.18.0 actual); a requirements file claiming a pin it did not make; and `uvicorn --workers 1` starting two processes, so a naive stop strands the worker on port 8000. The planned CI skip-marker for checkpoint tests turned out to be **unnecessary** - checking showed the suite already passes with the weights removed, so none was added | **The clean clone reproduced M7's Phase A output byte-for-byte** - sha256 `46bbf160e427...`, 1 089 939 bytes, fresh environment, three days later. Inference is deterministic and portable given a fixed adapter, which **does not contradict R14** (that concerns *training*). `peak_allocated_mb` 5143.73 is byte-identical across EXP-019b, EXP-034 and this run. **Generation total is 27**, not 25: 25 research + 1 M7 review + 1 M8 deployment validation, the last carrying no EXP-### and absent from the registry. Suites: **473 pytest / 169 vitest / 37 E2E**, eslint clean, build succeeds. The CI workflow is committed but **has never run** - nothing was pushed |
docs/07-testing-strategy.md:3:**Created:** 2026-07-27 · **Updated:** 2026-08-07 (M7 closure) · **Status:** **406 pytest tests and 165 vitest tests exist and pass.** The API and frontend-unit layers arrived with M7; the E2E (Playwright) layer is still outstanding and belongs to M8.
docs/07-testing-strategy.md:27:+5 `.env.example` guards, +20 E2E fixture-contract, +8 weights-manifest and +4 dataset-integrity
docs/deployment/runbook.md:129:npm run test                                # 169 vitest
docs/deployment/runbook.md:132:npx playwright test                         # 37 E2E, no GPU, no model
report/sources/15-testing.md:96:The suites grew from 406 pytest and 165 vitest at the MVP's close to

--- generation-count claims (authoritative: 27) ---
report/sources/00-front-matter.md:20:Numbers appearing in more than one place — test counts, the generation total, memory figures,
report/sources/06-methodology.md:99:| **GPU execution** | all {{ facts.generations_total }} generations. **Every one was explicitly authorised by Kylian Algoet; no AI assistant had authority to initiate GPU inference without that approval** |
report/sources/07-planning.md:84:contaminate a frozen matrix. The total is reported as **{{ facts.generations_total }}**.
report/sources/26-traceability-d1-d7.md:28:| 6 | Generate new decal artwork | {{ facts.generations_total }} real generations | **met** |
